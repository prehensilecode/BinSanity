def affinity_propagation(array,names,file_name,damping,iterations,convergence,preference,path,output_directory,binname) : ../bin/Binsanity-wf
def affinity_propagation(array,names,file_name,damping,iterations,convergence,preference,path,output_directory,outname) : ../bin/Binsanity
def affinity_propagation(array,names,file_name,damping,iterations,convergence,preference,path,output_directory,outname) : ../bin/Binsanity-refine
def affinity_propagation(array,names,file_name,damping,iterations,convergence,preference,path,output_directory) : ../test-scripts/Binsanity-lc
def checkm_analysis(file_,fasta,path) : ../bin/Binsanity-wf
def checkm_analysis(file_,fasta,path,prefix) : ../test-scripts/Binsanity-lc
def cov_array(a,b,name,size) : ../bin/Binsanity-refine
def cov_array(a,path,file_name,size) : ../bin/Binsanity
def cov_array(a,path,file_name,size) : ../bin/Binsanity-wf
def cov_array(a,path,file_name,size) : ../test-scripts/Binsanity-lc
def create_fasta_dict(fastafile) : ../bin/Binsanity
def create_fasta_dict(fastafile) : ../bin/Binsanity-refine
def create_fasta_dict(fastafile) : ../bin/Binsanity-wf
def create_fasta_dict(fastafile) : ../test-scripts/Binsanity-lc
def feature_counts(mapfile,outputdir,threads) : ../bin/Binsanity-profile
def format_for_feature(inputfile,mapfile,outputpath) : ../bin/Binsanity-profile
def GC_Fasta_file(b,name,outdir) : ../bin/Binsanity-refine
def GC_Fasta_file(b,name,prefix,location) : ../bin/Binsanity-wf
def GC_Fasta_file(b,name,prefix,location) : ../test-scripts/Binsanity-lc
def get_100x_log(file_,outfile) : ../bin/Binsanity-profile
def get_contigs(c,outdir,kmer) : ../bin/Binsanity-refine
def get_contigs(c,prefix,kmer,location) : ../bin/Binsanity-wf
def get_contigs(c,prefix,kmer,location) : ../test-scripts/Binsanity-lc
def get_cov_data(h) : ../bin/Binsanity
def get_cov_data(h) : ../bin/Binsanity-refine
def get_cov_data(h) : ../bin/Binsanity-wf
def get_cov_data(h) : ../test-scripts/Binsanity-lc
def get_log(file_,outfile) : ../bin/Binsanity-profile
def get_multiple(file_,outfile,num) : ../bin/Binsanity-profile
def get_squareroot(file_,outfile) : ../bin/Binsanity-profile
def kmean(array,names,filename,path,clusters,path2,prefix,threads) : ../test-scripts/Binsanity-lc
def kmer_counts(kmer,b,name) : ../bin/Binsanity-refine
def kmer_counts(kmer,b,name) : ../bin/Binsanity-wf
def kmer_counts(kmer,b,name) : ../test-scripts/Binsanity-lc
def kmer_list(dna, : ../bin/Binsanity-refine
def kmer_list(dna, : ../bin/Binsanity-wf
def kmer_list(dna, : ../test-scripts/Binsanity-lc
def make_coverage(ids,outfile,outputdir) : ../bin/Binsanity-profile
def output(a,outdir,kmer) : ../bin/Binsanity-refine
def output(a,prefix,kmer,location) : ../bin/Binsanity-wf
def output(a,prefix,kmer,location) : ../test-scripts/Binsanity-lc
def refined_ap(array,names,file_name,damping,iterations,convergence,preference,path,output_directory) : ../bin/Binsanity-wf
def refined_ap(array,names,file_name,damping,iterations,convergence,preference,path,output_directory,prefix) : ../test-scripts/Binsanity-lc
def run_checkM(bins,threads,prefix,directory) : ../bin/Binsanity-wf
def run_checkM(bins,threads,prefix) : ../test-scripts/Binsanity-lc
